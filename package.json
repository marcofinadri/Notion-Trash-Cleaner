{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "zip": "npm run build && cd dist && zip -r ~/Desktop/notion-trash-cleaner.zip . -x '*.DS_Store'"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
